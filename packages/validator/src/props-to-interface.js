/**
 * a new method to help work with @type
 * take the props (for configurator) then turn them into proper interface
 * by default everything will be optional, but you can modify it when done
 * @param {object} props the configurator props
 * @return {object} the interface style json
 */
export default function propsToInterface(props) {
  // @TODO
}
