import test from 'ava'
import { JsonqlContract } from '../src'
import { join } from 'node:path'

const testClassFile = join(__dirname, 'fixtures', 'velocejs', 'test-class.ts')

test(`Should able to generate the basic contract`, async t => {
  

})
